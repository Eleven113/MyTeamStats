{% extends "layout.html.twig" %}

{% block head %}
    <title>Modifier un match - MyTeamStats</title>
{% endblock %}

{% block content %}

    <div id="updatematch_form" class="d-flex flex-column justify-content-center align-items-center">
        <form id="updatematch-form" class="col-lg-6" action="/MyTeamStats/UpdateMatch"  method="post" enctype="multipart/form-data">
            <legend>Créér un match</legend>
            <div class="form-group">
                <label for="text">Date</label>
                <input id="text" type="datetime-local" name="date" class="form-control" value="{{ match.date }}" required>
            </div>
            <div class="form-group">
                <label for="text">Adversaire</label>
                <select name="opponent">
                    {% for oppo in oppoListObj %}
                        <option value="{{ oppo.opponentid }}">{{ oppo.name }}</option>
                    {% endfor %}
                </select>
            </div>
            <div class="btn btn-group btn-group-toggle" data-toggle="buttons" aria-pressed="false">
                <label class="btn btn-secondary active">
                    <input type="radio" name="athome" value="1" id="1" autocomplete="off" checked> Domicile
                </label>
                <label class="btn btn-secondary">
                    <input type="radio" name="athome" value="0" id="0" autocomplete="off"> Extérieur
                </label>
            </div>
            <div class="form-group">
                <label for="text">Terrain</label>
                <select name="fieldid">
                    {% for field in fieldsListObj %}
                        <option value="{{ field.fieldid }}">{{ field.name }} / {{ field.address }} / {{  field.city }} / {{ field.turf }}</option>
                    {% endfor %}
                </select>
            </div>
            <div class="form-group">
                <label for="text">Catégorie</label>
                <select name="category">
                    <option value="U18">U18</option>
                    <option value="U15">U15</option>
                </select>
            </div>
            <div class="form-group">
                <label for="text">Compétition</label>
                <select name="type">
                    <option value="championship">Championnat</option>
                    <option value="cup">Coupe</option>
                    <option value="futsal">Futsal</option>
                    <option value="tournament">Tournoi</option>
                    <option value="friendly">Amical</option>
                </select>
            </div>
            <div class="form-group">
                <label for="text">Nombre de période</label>
                <select name="periodnum">
                    <option value="1">1</option>
                    <option value="2">2</option>
                    <option value="4">4</option>
                </select>
            </div>
            <div class="form-group">
                <label for="text">Durée d'une période</label>
                <input id="text" type="number" name="periodduration" class="form-control">
            </div>
            <button type="submit" class="btn btn-primary"><i class="fas fa-check-circle"></i>&nbsp;&nbsp;Envoyer</button>
        </form>
    </div>


{% endblock %}